kind = "web"
previewPath = "/"
title = "Unity Global Stake"
version = "1.0.0"
id = "artifacts/usg-stake"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26265

[services.development]
run = "pnpm --filter @workspace/usg-stake run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/usg-stake", "run", "build" ]
run = "pnpm --filter @workspace/usg-stake run start"

[services.env]
PORT = "26265"
BASE_PATH = "/"
